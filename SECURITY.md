# 安全策略 / Security Policy

vllm_kestrel 是一个边缘 LLM 推理引擎，自带国密实现（SM3 / SM4-CTR / HMAC-SM3 / SM2）、
权重加密与签名、可验证推理（attestation）等安全能力。
**我们非常欢迎安全研究者对它做对抗性审查。**

---

## 一、如何报告漏洞（请勿开公开 Issue）

> ⚠️ **请不要在 Gitee Issues 中公开安全漏洞的细节。** 公开即等于给所有部署者 0 天。

请通过以下**私下渠道**报告：

- **邮箱**：398152090@qq.com（请在主题加前缀 `[SECURITY]`）

若能提供加密通信方式需求，可在邮件中说明，我们会回复协商。

**我们承诺**：在修复发布前，不会公开你的身份与漏洞细节（除非你希望署名）。

---

## 二、报告请尽量包含

| 项 | 内容 |
|---|---|
| 受影响版本 | 源码 commit（`git rev-parse --short HEAD`）或发布版本号 |
| 受影响的组件 | 例如：`vqf.c` 解密路径 / `vllm_attest.c` 出证 / SM2 验签 / HTTP 服务面 / 管理页 |
| 漏洞类型 | 例如：密钥泄漏、认证绕过、内存安全（越界/释放后使用）、验签绕过、信息泄漏 |
| **影响与前置条件** | 攻击者需要什么条件？能否远程触发？是否需要本地权限？ |
| **可复现步骤** | 最小复现（命令 + 输入 + 观察到的行为）；有 PoC 更好 |
| 你的研判 | 严重性评估（可参考 CVSS） |
| 建议修复方向 | 可选，但很有帮助 |

**最小可用报告 = 版本 + 组件 + 复现步骤 + 影响。** 不必等写出完整报告再联系我们。

---

## 三、本项目的安全面（我们最希望你审查的地方）

按优先级列出，便于你选择切入点：

1. **国密实现**
   - SM3 / SM4-CTR / HMAC-SM3 / SM2 均为**自研实现**（无第三方密码库）；
   - 我们以**国密标准 KAT 向量**逐字节验证正确性，但**自研实现的旁路与实现缺陷仍是重点风险**；
   - 特别关注：SM2 随机数源与 **k 复用**问题、SM4-CTR 计数器管理、HMAC 比较是否恒定时间。
2. **权重加密与签名（VQF-Enc / SM2 内嵌签名）**
   - 单遍解密路径与 `mprotect` + 写时复制；
   - **签名覆盖分析**：是否存在未被签名覆盖的字段（可被掉包）；
   - 私钥文件的权限校验与密钥目录的文件系统要求（本项目已加入权限回读校验与非 POSIX
     文件系统告警，欢迎验证其充分性）。
3. **可验证推理（attestation）**
   - 出证帧规范（`body_sha` 与输出字节绑定）；
   - **离线验签绕过**：能否构造一个通过验签但不反映真实推理的凭证？
   - 凭证与请求原文的绑定是否可被替换（重放 / 拼接）。
4. **内存与并发安全**
   - 自研线程池 `vllm_tp`、连续批处理、L3 分层驻留的落盘/回填路径；
   - mmap 直挂与 `MADV_DONTNEED` 驱逐是否可能触发越界或释放后使用；
   - 我们已在板端以 `-fsanitize=address` 跑通主路径（0 错误），但**非常规输入路径**更值得测。
5. **服务面**
   - 自研 select 轮询 HTTP + SSE + 最小 JSON 解析器；
   - 管理页 / 对话页（内嵌 HTML）与 `/admin/api/*` 接口的鉴权与注入面；
   - 默认**无鉴权**的设计前提是"部署在内网/可信网络"——**如果你的部署暴露在公网，
     请视为高危配置**，并优先反馈你发现的边界问题。
6. **分布式专家并行（EP）的跨机面**
   - [src/core/vllm_ep.c](src/core/vllm_ep.c) 是一个裸 TCP 服务（`AF_INET` / `SOCK_STREAM`），
     用于在 EP 各 rank 之间交换 MoE 专家的中间结果（`--moe-ep-coord` / `--moe-ep-worker`）；
   - **该协议没有鉴权、没有完整性校验，也没有魔数与版本号**——任何能连上该端口的主机
     都可参与张量交换。这是**面向可信 LAN 的算力拼接**所做出的有意取舍（不面向公网），
     我们在此明确披露，并欢迎你评估它的边界；
   - 默认 `--ep-host 127.0.0.1`（仅监听本机）；跨机部署必须显式绑定板 IP，此时该端口
     对整个局域网开放；
   - 协议内只实现了超时（accept / connect 与 socket 读写超时），属**可用性**防线，
     不是安全防线。

---

## 四、响应时间线（我们的承诺）

| 阶段 | 目标 |
|---|---|
| 首次回应 | **3 个工作日内**（确认收到并给出初步分类） |
| 初步研判 | **7 个工作日内**（确认/否认可复现，给出严重性初判） |
| 修复计划 | 与报告人协商后给出时间表；严重问题优先修复并尽快发布 |
| 公开披露 | 修复发布后，与报告人协商披露时间与署名方式 |

若我们**无法复现**，会说明我们的环境与前提，并请你补充差异项——**不会简单忽略或关闭**。

---

## 五、明确不在范围内（避免误解）

1. **模型权重与数据集本身的安全性**：权重不随仓库分发，其来源与许可是使用者自己的责任；
2. **第三方组件的已知问题**：本项目仅含两个 MIT 数据面组件（`stb_image.h`、
   llama.cpp 派生 4x4 asm GEMM），其自身漏洞请反馈给上游；
3. **x86-64 分支**：该分支**仅用于功能自检与位级一致性对照，不作性能与安全基准**；
4. **部署侧配置不当**：例如把无鉴权的管理接口直接暴露公网、密钥文件放在共享目录等；
   这类我们会给出加固建议，但不视为引擎漏洞；
5. **纯拒绝服务（DoS）类**：作为单机推理引擎，其算力本身就是资源上限；
   除非你能证明存在**非预期**的资源放大（例如一个请求导致远超常理的资源占用），否则不在范围。
6. **EP 跨机协议"无鉴权"这一设计本身**：见「三、安全面」第 6 条，这是面向可信 LAN 的明确取舍；
   但若你能证明它在**默认配置**（`--ep-host 127.0.0.1`）下即可被非本机利用，那正是需要我们
   立刻处理的问题，欢迎报告。

---

## 六、部署加固速查（给使用者）

即使没有漏洞，错误的部署方式也会很危险。请至少做到：

- ☐ **不要把管理接口与推理接口直接暴露公网**；如需暴露，请置于反向代理 + 鉴权之后；
- ☐ **跨机 EP（`--moe-ep-coord` / `--moe-ep-worker`）只在可信网络内使用**：该端口
  **无鉴权、无完整性校验**，默认仅监听 `127.0.0.1`；跨机请用网段隔离或隧道，
  不要暴露到公网或不受控的局域网；
- ☐ **密钥文件目录使用 POSIX 权限语义的文件系统**（本项目对非 POSIX 文件系统会告警）；
- ☐ 密钥文件权限最小化，并定期核验（本项目已加入权限回读校验）；
- ☐ 如需权重保密，使用 **VQF-Enc 加密权重 + SM2 签名**；
  注意**此时不能用逐层推理**（逐层档只支持明文 VQF，会显式拒绝加密权重）；
- ☐ 需要"推理过程可被第三方验证"时，启用 attestation 并用配套离线验签工具核验；
- ☐ 关注发布页的安全修复说明，及时更新。

---

## 七、致谢

我们会在修复发布后（经你同意）在发布说明中致谢报告人。
**感谢你帮助这个边缘推理引擎变得更可信。**
