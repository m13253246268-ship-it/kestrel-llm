# 贡献指南 / Contributing to vllm_kestrel

感谢你愿意为 Kestrel（`vllm_kestrel`）贡献。本项目是**纯 C11、零第三方运行时**的
ARM（aarch64）CPU LLM 推理引擎，因此对贡献的期待也偏"工程化"：
**能复现、能自证、口径写全**。

> 一句话：**我们更欢迎一份写清口径的复现报告，而不是一句"跑出来不对"。**

---

## 一、你可以贡献什么

| 类型 | 例子 | 价值 |
|---|---|---|
| **真 Bug** | 崩溃、结果错误、内存错误、位级不一致 | 帮我们修代码 |
| **口径偏差** | "复现不出文档里的 2.0 s"（往往是配置档 / 口径没对齐） | 帮我们知道读者在哪一步摔跤 |
| **文档不清** | 某处表述与源码对不上、参数含义没写全 | 帮我们修文档 —— **这也是真贡献** |
| **性能数据** | 你在别的板子上（其他 aarch64 / 大核服务器）跑出的数据 | 帮我们划清结论的适用边界 |
| **代码 / 文档 PR** | 内核优化、bug 修复、翻译、教程 | 见第四节流程 |

**报告你发现的坑不是"冒犯"，是贡献。**

---

## 二、提 Issue：请带上"可复现三件套"

作者看到一条 Issue，第一反应永远是三个问题：**你跑的是什么？你给了什么参数？你看到什么？**
请让 Issue 自带答案：

```markdown
### 1. 环境
- 板型 / 设备画像：<例如 Orange Pi 5 Plus / RK3588 / 4×A76+4×A55>
- 架构：<aarch64 | x86-64>（注意：x86-64 分支只作功能自检，不作性能基准）
- 内存：<MemTotal，例如 16,347,860 kB = 15.6 GiB>
- 权重所在介质：<eMMC | microSD | NVMe，附实测带宽更佳>
- 编译方式：<./build_rk3588.sh | --static | --cross> 
- 源码 commit：<git rev-parse --short HEAD>
- 引擎 sha256：<sha256sum vllm_kestrel>

### 2. 复现步骤
<完整命令，含模型目录与全部参数；环境变量也要写>

### 3. 期望 vs 实际
- 期望：<...>
- 实际：<贴日志原文，不要转述>

### 4. 对照的文档
<我按 docs/xxx 的第 y 节做了 z，结果与文档不符>
```

**第 4 段是最高价值的一段。** 很多"bug"其实是"文档口径没读全"；
先写清"我对照了哪一节"，作者一眼就能判断是**文档 bug**、**代码 bug** 还是**操作偏差**。

> 提示：标题请带模块标签，例如 `[vqf]`、`[l3]`、`[attest]`、`[bench]`、`[serve]`。

---

## 三、性能类 Issue 的额外要求：口径必须写全

本项目对性能数字有**强制口径纪律**（详见 `wiki/术语与数据口径.md`）。
提性能类 Issue 时，请至少标注以下四项，否则作者无法判断差异来源：

| 必标项 | 说明 |
|---|---|
| **线程数** | `--threads 4` 与 `--threads 8` 在 8B 全层 decode 上差 **2.3×**（RK3588 上 `--threads 8` 会把 4 个 A55 小核拉进 GEMM 并行区） |
| **页缓存状态** | 冷页缓存（`sync; echo 3 > /proc/sys/vm/drop_caches`）还是热缓存 |
| **内存口径** | 纯权重 RSS（`--stream-test`）还是含 KV 的 serve 峰值（VmHWM） |
| **版本测点** | v0（2026-09-05）还是 v1.0（2026-09-12）；**两代口径不同，禁止混比** |

> 关于 `VmHWM`：它是进程**历史最高水位**（单调不减），不是单次请求的稳态占用。
> 引用时请说明是"该次实测时刻的水位"还是"N 次累计后的高水位"。

---

## 四、提 PR

### 4.1 最小要求

1. **必须先过自检门**：

   ```bash
   ./build_rk3588.sh --run-tests      # 构建 + PASS/FAIL 自检（退出码 0/1）
   ```

   涉及 L3 / 稀疏注意力 / KV 的改动，请额外跑对应判定式自检：

   ```bash
   ./vllm_kestrel --test-l3
   ./vllm_kestrel --test-sparse
   ```

2. **涉及内存安全的改动**，请以 ASan 构建跑一遍主路径：

   ```bash
   cmake -B build-asan -DCMAKE_BUILD_TYPE=Debug \
     -DCMAKE_C_FLAGS="-fsanitize=address -fno-omit-frame-pointer"
   cmake --build build-asan -j
   # 目标：0 条内存错误报告
   ```

3. **涉及数值路径的改动**，请说明是否影响位级确定性——
   本项目的红线是：`--stream-test` 的贪心 TOKIDS 序列在同一模型的不同驻留档下**逐位一致**。

### 4.2 PR 描述模板

```markdown
### 改了什么 / 为什么
### 影响的模块与文件
### 怎么验证的
- [ ] ./build_rk3588.sh --run-tests 通过
- [ ] 相关判定式自检通过（--test-l3 / --test-sparse / ...）
- [ ] 涉及数值路径时：位级一致性未回归
- [ ] 涉及性能宣称时：附同口径对照表（线程数 / 冷热页缓存 / 内存口径 / 版本）
### 是否需要同步文档
```

### 4.3 代码风格

- **C11**，不引入第三方运行时依赖（这是本项目的立身之本，PR 若引入新依赖会被要求说明必要性）；
- 内核层改动请保持手写 NEON 路径的可读性，必要时附标量回退实现；
- 新增文件请在文件头加 SPDX 标识：

  ```c
  /* SPDX-License-Identifier: AGPL-3.0-or-later
   * Copyright (C) 2026 裴晓光 and contributors */
  ```

- 提交信息建议使用 `fix(模块):` / `feat(模块):` / `docs:` / `bench:` 前缀（与本仓库历史一致）。

---

## 五、我们承诺什么

| 承诺 | 内容 |
|---|---|
| 复现类 Issue | **7 个工作日内**给出首次回应（含"已复现 / 未能复现 / 需要更多信息"） |
| 安全类报告 | 见 [SECURITY.md](SECURITY.md)（私下报告，**不要**开公开 Issue） |
| 文档类 Issue | 一并欢迎；文档修补与代码修补同等对待 |
| 未能复现时 | 我们会说明我们的复现环境与口径，并请你补充差异项，**不会简单地关闭** |

---

## 六、许可与贡献者条款

本项目采用**双许可**：**AGPL-3.0-or-later** 或**商业许可**（见 [LICENSING.md](LICENSING.md)）。

提交 PR / 补丁即表示你同意：**你的贡献以本项目的双许可条款发布**，
且你确认该贡献为你原创或已获得相应授权（即"开发者原创证书（DCO）"精神：
你有权提交，且同意按本项目许可分发）。

如你的贡献涉及第三方代码，请在 PR 中明确标注其来源与许可，并确保与本项目许可兼容。

---

## 七、复现数据与联系

- 复现脚本：`tools/bench/bench_value.sh`（**仅依赖 Python 3 标准库**）
- 数据口径：`wiki/术语与数据口径.md`
- 排障手册：`docs/` 五篇（技术文档 / 基准报告 / 优化配置与边界 / KV v2 方案 / 权重保护与可验证推理）
- 联系邮箱（研究合作 / 复现数据 / 商业授权）：**398152090@qq.com**

再次感谢——**每一个写清口径的 Issue，都在让下一个使用者少踩一次坑。**
